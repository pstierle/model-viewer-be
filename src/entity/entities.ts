import { Model } from './entities/model.entity';
import { PointOfInterest } from './entities/point-of-interest.entity';
import { User } from './entities/user.entity';

export const entities = [Model, PointOfInterest, User];
